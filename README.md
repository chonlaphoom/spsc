# Structure

spsc -> Single-producer Sigle-consumer FIFO Queue

libs -> this is my dojo to create any abitrary libraries

# Tests

use munit

munit is a git submodule; after cloning run `git submodule update --init`.
Build & run a lib's tests with `make test` (see `libs/intStack/Makefile`).
